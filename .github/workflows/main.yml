name: CloudFlare

on:
  push:
    branches:
      - main
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v4
      - name: Deploy app
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: "APRvK1xnqkInAustRenZ8umsMUnycpn9s3hsI10V"
          accountId: "04bbb7e0b5ef171609909c677047c263"
          command: deploy